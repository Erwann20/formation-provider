overwrite: true
schema: "https://countries.trevorblades.com"
documents: "./**/*.graphql"
generates:
  src/gql/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typed-document-node"
